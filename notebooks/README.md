# Notebooks

| Notebook | Purpose | Generated by |
|---|---|---|
| `benchmark_report.ipynb` | Naive RAG vs Apex RAG charts | `make benchmark` |
| `hnsw_sweep.csv`         | recall@10 vs latency over `ef_search` | `python -m apex.scripts.hnsw_tune` |
| `quantization_report.csv` | float32 vs int8 vs binary embeddings | `python -m apex.scripts.quantize_embeddings` |

Open them with JupyterLab:

```bash
pip install jupyterlab
jupyter lab notebooks/
```
